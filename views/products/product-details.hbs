<h1>{{productFound.name}}</h1>

<h2>Sold by: {{productFound.seller.username}}</h2>

<h2>Price: {{productFound.price}}€</h2>

<h3>min Age: {{productFound.minAge}}</h3>

<h3>max Age: {{productFound.maxAge}}</h3>

<p>description: {{productFound.description}}</p>

<img src="{{productFound.image}}" alt="">


<form action="/products/{{productFound._id}}/delete" method="POST"><button>Delete</button></form>
<form action="/products/{{productFound._id}}/edit"><button>Edit</button></form>
{{#if isFavourite}}
<form action="/products/{{productFound._id}}/removefavourite" method="POST"><button id="remove-favourite">Remove from Favourites!</button></form>
{{else}}
<form action="/products/{{productFound._id}}/addfavourite" method="POST"><button id="add-favourite">Add to Favourites!</button></form>
{{/if}}





